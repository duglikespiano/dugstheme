const variableString = 'foo';
const variableBoolean = true;
const variableObject = {
	name: 'foo',
	age: 10,
};
const variableArray = [1, 2, 3, 4, 5];
const htmlElement = document.querySelector('.dummy');
const dummyFunction = () => {
	htmlElement.classList.add('dummy');
	return 'bar';
};
